packages:
  - apps/*
  - packages/*

# Native build scripts are blocked by default (pnpm >= 10); only reviewed
# packages are allowed (pnpm 11 uses the allowBuilds map).
allowBuilds:
  esbuild: true

minimumReleaseAgeExclude:
  - marked@18.0.12
